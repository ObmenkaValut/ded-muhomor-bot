# 🍄 Дед Мухомор — Telegram-бот

Ворчливый дед-маразматик в групповом чате магазина биодобавок. Иногда буркнет что-нибудь.

## Стек

- TypeScript + Node.js
- [grammY](https://grammy.dev/) — Telegram Bot Framework
- [Google Gemini Flash](https://ai.google.dev/) — генерация ответов
- Railway — деплой (long polling)

## Установка

```bash
# Клонируем и устанавливаем зависимости
sudo npm install

# Создаём .env файл из примера
cp .env.example .env
```

Заполни `.env` своими ключами:

```env
BOT_TOKEN=123456:ABC...       # Telegram bot token от @BotFather
GEMINI_API_KEY=AIza...        # Google Gemini API key
BOT_USERNAME=DedMuhomorBot    # Username бота без @
```

## Запуск

```bash
# Локальная разработка (hot-reload)
npm run dev

# Продакшен
npm start
```

## Деплой на Railway

1. Создай новый проект на [railway.app](https://railway.app)
2. Подключи GitHub-репозиторий
3. Добавь переменные окружения в настройках сервиса:
   - `BOT_TOKEN`
   - `GEMINI_API_KEY`
   - `BOT_USERNAME`
4. Railway автоматически определит `npm start` как стартовую команду
5. Деплой произойдёт автоматически

## Структура проекта

```
src/
├── index.ts              — точка входа, инициализация бота
├── config/
│   └── constants.ts      — константы (лимиты, шансы, кулдауны)
├── services/
│   ├── gemini.ts         — вызов Gemini API
│   └── messageBuffer.ts  — буфер сообщений per chat
└── handlers/
    └── groupMessage.ts   — обработка сообщений в группе
```

## Как работает

- Бот слушает текстовые сообщения в группах/супергруппах
- Хранит последние 30 сообщений для контекста
- **Реплай на бота / упоминание** → отвечает обязательно
- **Обычное сообщение** → 30% шанс ответить
- Кулдаун 30 секунд между ответами (anti-spam)
- Gemini генерирует короткие ворчливые реплики от лица Деда Мухомора
